# DCONF configuration

DCONF configuration for Ubuntu (gnome)

## Installation

```bash
cat dconf-settings.ini | dconf load / 
```

# Refresh config file

```bash

dconf dump / > dconf-settings.ini 
```

